# Veröffentlicht ausschließlich dist/, und nur nach bestandener Prüfung.
# Voraussetzung bei GitHub: Settings → Pages → Source: GitHub Actions.
name: Website veröffentlichen

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  pruefung:
    uses: ./.github/workflows/pruefung.yml

  paket:
    needs: pruefung
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/configure-pages@v5
      # Offene Platzhalter sind beim Arbeiten erlaubt, in der Auslieferung nicht.
      - name: Keine offenen Platzhalter veröffentlichen
        run: tools/pruefen.sh --veroeffentlichung
      - name: Veröffentlichungsdateien erzeugen
        run: |
          tools/build.sh
          tools/build.sh --check
      - uses: actions/upload-pages-artifact@v4
        with:
          path: dist

  veroeffentlichung:
    needs: paket
    runs-on: ubuntu-latest
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Geprüfte Website veröffentlichen
        id: deployment
        uses: actions/deploy-pages@v4
