#!/bin/sh
# Prüft den Arbeitsbaum, ohne Quellen, dist/ oder Git-Index zu verändern.
# Bei teilweise vorgemerkten Änderungen prüft die CI zusätzlich den Commit.
set -eu
cd "$(git rev-parse --show-toplevel)"
exec tools/pruefen.sh
